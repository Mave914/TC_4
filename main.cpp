#include <iostream>
using namespace std;
int main()
{
    //ofstream salida("salida.txt")
    //ifstream entrada ("entrada.txt")

    /*
     while(){
     int = 1;
     salida <<n++<<"."<<endl;
     }

     while(getline(entrada, linea)
    salida <<n++<<"."<<endl;

    entrada.close();
    salida.close();
     */

    return 0;
}